/* eslint-disable prettier/prettier */
/* eslint-disable react/prop-types */
import React from 'react';

const Title = (props) => {
  return <h3 className="contentTitle">{props.title}</h3>;
};

export default Title;
