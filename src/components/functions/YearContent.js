/* eslint-disable prettier/prettier */
import React from 'react';

const YearContent = (props) => {
  // eslint-disable-next-line react/prop-types
  return <p className="yearContent">{props.year}</p>;
};

export default YearContent;
